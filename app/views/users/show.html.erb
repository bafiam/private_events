<nav class="navbar navbar-light sticky-top" style="background-color: #e3f2fd;">
  <!-- Navbar content -->
  <ul class="navbar-nav mr-auto list-unstyled">
      <li class="nav-item active" style="padding:15px;"><%= link_to 'Create a new Event', new_event_path %>
      </li>
      <li class="nav-item"style="padding:15px;"><%= link_to 'All Events', events_path %>
      </li>
      <li class="nav-item"style="padding:15px;"><%= link_to 'Booked Events', event_path(current_user)  %>
      </li>
      <li class="nav-item"style="padding:15px;"><%= link_to 'Envite Attendees', new_booking_path  %>
      </li>

    </ul>
</nav>
<div class="container-fluid">

<h1>Profile</h1>
<p>Welcome <b><%= @user.name %></b></p>
<h3 style="margin-left:380px">Your created events</h3>

<%if @user.events[0].nil?%>
  <li>You have not created any event. Create one first!!</li>
<%else%>
  <% @user.events.each do |event| %> 
    <table class="table table-dark">
  <thead class="thead-dark">
    <tr>
      <th scope="col">#</th>
      <th scope="col">Event name:</th>
      
    </tr>
  </thead>
  <tbody>
    <tr>
      <th scope="row">1</th>
      <td><%= event.name%></td>
    </tr>
  </tbody>
  
</table>
  <% end %>
  <%end%>


<h3 style="margin-left:380px">Your attended events</h3>

<h3>Upcoming events</h3>
<%@upcoming_events.each do |event| %>
<table class="table table-dark">
  <thead class="thead-dark">
    <tr>
      <th scope="col">#</th>
      <th scope="col">Event name:</th>
      <th scope="col">Event description:</th>
      
    </tr>
  </thead>
  <tbody>
    <tr>
      <th scope="row">1</th>
      <td><%= event.name%></td>
      <td><%= event.description%></td>
    </tr>
  </tbody>
  
</table>
<% end %>

<h3>Previous events</h3>
<%@prev_events.each do |event| %>
<table class="table table-dark">
  <thead class="thead-dark">
    <tr>
      <th scope="col">#</th>
      <th scope="col">Event name:</th>
      <th scope="col">Event description:</th>
      
    </tr>
  </thead>
  <tbody>
    <tr>
      <th scope="row">1</th>
      <td><%= event.name%></td>
      <td><%= event.description%></td>
    </tr>
  </tbody>
  
</table>
<% end %>
</div>


